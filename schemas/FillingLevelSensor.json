{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ngsi-ld-tutorials/generated/FillingLevelSensor/schema.json",
  "title": "FillingLevelSensor",
  "description": "Sensor/actuator for a feed or water trough; add/remove commands adjust the filling level. Baseline generated from 4 live entities on tenant 'openiot' plus the /types attribute list.",
  "x-derivedFrom": {
    "method": "entity-introspection + /types",
    "parent": "Device",
    "instances": 4
  },
  "type": "object",
  "allOf": [
    {
      "$ref": "./Device.json"
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "FillingLevelSensor"
          ],
          "description": "NGSI Entity Type. It has to be FillingLevelSensor"
        },
        "add": {
          "x-ngsi-type": "Command",
          "description": "Actuation command (write-only; not returned on read)."
        },
        "remove": {
          "x-ngsi-type": "Command",
          "description": "Actuation command (write-only; not returned on read)."
        }
      },
      "required": [
        "id",
        "type"
      ]
    }
  ]
}
